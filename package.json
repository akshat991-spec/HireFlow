{
  "name": "hireflow",
  "version": "1.0.0",
  "description": "HireFlow — Hiring Pipeline Management Platform (React, Vite, Express, TypeScript, PostgreSQL)",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n \"server,client\" -c \"blue,green\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:server": "npm run dev --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:client": "npm run dev --prefix frontend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "build:server": "npm run build --prefix backend",
    "build:client": "npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "db:migrate": "npm run db:migrate --prefix backend",
    "db:seed": "npm run db:seed --prefix backend",
    "test": "npm run test --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
